import { NgbDateStruct } from '@ng-bootstrap/ng-bootstrap';

export class NgbDateStructEx implements NgbDateStruct {
    year: number;
    month: number;
    day: number;

    hour: number;
    minute: number;
}